{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - Quidieance{% endblock %}

{% block content %}

<div class="content py-3">
    <div class="message Py-3">
        {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}
    </div>
    <div class="py-3">
        <a href="{{ path('compte') }}" class="btn btn-primary mt-3">Retour a la page mon compte</a>
    </div>
    
    <div class="jumbotron text-center py-3">
        <h1>Modifier mon mot de passe</h1>
    </div>
        <hr>
        <div class="container py-3">
            <div class="py-5 bg-dark  mg-3 border border-dark text-center text-light">
                {{ form(form)}}      
            </div>       
        </div>  
    

</div>

    <hr>
{% endblock %}
