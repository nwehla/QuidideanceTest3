{% extends 'base.html.twig' %}

{% block title %}Nouveau sondage{% endblock %}

{% block content %}
<div class="container py-3">
    <div class="py-3">
        <a href="{{ path('app_survey_index') }}" class="btn btn-primary mt-3 btn-outline-primary">Retour au tableau </a>
    
    </div>
        <div class="jumbotron text-center py-3">
            <h1>Ajout d'un nouveau</h1>
        </div>
        <div class="py-3 width 30%">
            {{ include('survey/_form.html.twig') }}
    
        </div>
    
</div>

{% endblock %}
