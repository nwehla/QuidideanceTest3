{% extends "base.html.twig" %}

{% block content %}
<div class="container py-3">

</div>
<div class="py-3">
  <h1><a class="btn btn-outline-primary" href="{{path('app_sondage_index')}}">Sondage !</a>
</div>
    <table class="table table-striped bordored-radius=2px text-center ">
      <thead class="table table-danger">
          <tr>
                <th>Titre</th>
                <th>Categorie</th>
            
              </tr>
            </thead>
            <tbody>
        {% for sondage in sondages %}
        <tr>
    
        <td>
          {{sondage.titre}}
        </td>
        
       <td>
          {{sondage.categorie.titre}}
        </td>
       <td>
          {{sondage.interrogers.intitule}}
        </td>
       <td>
          {{sondage.reponses.titre}}
        </td>
        </td>
        
{% endfor %}
        </tbody>
    </table>

{% endblock %}
