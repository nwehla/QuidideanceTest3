<form method="post" action="{{ path('app_interroger_delete', {'id': interroger.id}) }}" onsubmit="return confirm('Êtes vous sûr(e) de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interroger.id) }}">
    <button class="btn btn-warning mt-3 btn-outline-danger">Supprimer</button>
</form>
