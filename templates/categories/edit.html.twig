{% extends 'base.html.twig' %}

{% block title %}Modification des Categories{% endblock %}

{% block content %}
{% for message in app.flashes('success') %}
<div class="alert alert-success">
    {{ message }}
</div>
{% endfor %}
<div class="container py-3">
    <div class="py-3">
        <a href="{{ path('app_categories_index') }}" class="btn btn-primary mt-3">Retour au tableau des categories</a>
    
    </div>
    <div class="jumbotron py-3 text-center">
        <h1>Modification de la Categorie</h1>
    
    </div>
    
    <div class="container py-3">
        {{ include('categories/_form.html.twig', {'button_label': 'Modifier'}) }}

        
    </div>
    
    
        {# {{ include('categories/_delete_form.html.twig') }} #}
    
</div>
{% endblock %}
